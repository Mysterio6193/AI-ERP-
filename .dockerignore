# version control
.git
.gitignore

# dependencies & build outputs
node_modules
.next
apps/driver-app/.next
.pgdata

# secrets and env files (except example)
.env
.env*.local
!.env.example

# databases — runtime SQLite DBs (Postgres data is in volume)
*.db
*.db-journal
*.db-wal
*.db-shm
db/
prisma/db/

# runtime uploads
public/uploads/

# logs
*.log

# docs / scratch / archives
docs.zip
*.zip
upload/
download/

# local AI-tooling config
.claude
.codex
skills

# tests
test
coverage
